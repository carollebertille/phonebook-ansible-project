---
- name: create project directory
  file:
    path: "{{ home }}/phonebook-project"
    state: directory
  tags:
    - build
    - preprod
    - prod

- name: copy devops private key file
  copy:
    content: "{{ github_private_key }}"
    dest: "{{ home }}/.ssh/id_rsa"
    owner: centos
  tags:
    - build
    - preprod
    - prod

- name: load the source code in github
  git:
    repo: "{{ repo_phonebook  }}"
    dest: "{{ home }}/phonebook-project"
    accept_hostkey: yes
    force: yes
    recursive: no
    key_file: "{{ home }}/.ssh/id_rsa"
    version: "{{ branch_clone }}"
  tags:
    - build
    - preprod
    - prod
    
- name: Log into DockerHub
  docker_login:
    username: "{{ dockerhub_login_vault }}"
    password: "{{ dockerhub_pwd_vault }}"
  tags:
    - build
    - preprod
    - prod

- name: Create a global network of our two containers
  docker_network:
    name: phonebook_network
  tags:
    - preprod
    - prod

- block:
  - name: Build phonebook-mysql
    docker_image:
      build:
        path: "{{ home }}/phonebook-project"
        dockerfile: Dockerfile-mysql
      name: "{{ username }}/phonebook-mysql"
      state: present
      source: build
      tag: "{{ tag_version }}"

  - name: Build phonebook-app
    docker_image:
      build:
        path: "{{ home }}/phonebook-project"
        dockerfile: Dockerfile-app
      name: "{{ username }}/phonebook-app"
      state: present
      source: build
      tag: "{{ tag_version }}"
  tags:
    - build

- block:
  - name: push phonebook-mysql
    docker_image:
      build:
        path: "{{ home }}/phonebook-project"
        dockerfile: Dockerfile-mysql
      name: "{{ username }}/phonebook-mysql"
      state: present
      source: build
      push: yes
      tag: "{{ tag_version }}"

  - name: push phonebook-app
    docker_image:
      build:
        path: "{{ home }}/phonebook-project"
        dockerfile: Dockerfile-app
      name: "{{ username }}/phonebook-app"
      state: present
      source: build
      push: yes
      tag: "{{ tag_version }}"
  tags:
    - push


- block:
  - name: pull image of phonebook-mysql
    docker_image:
      name: "{{ username }}/phonebook-mysql:{{ tag_version }}"
      source: pull

  - name: pull image of phonebook-app
    docker_image:
      name: "{{ username }}/phonebook_repo/phonebook-app:{{ tag_version }}"
      source: pull
  tags:
    - prod


- block:
  - name: container phonebook mysql
    docker_container:
      name: phonebook-mysql
      image: "{{ username }}/phonebook-mysql:{{ tag_version }}"
      state: started
      restart: yes
      networks:
        - name: phonebook_network

  - name: sleep for 20 seconds and continue with play
    wait_for:
      timeout: 20

  - name: container phonebook app
    docker_container:
      name: phonebook-app
      image: "{{ username }}/phonebook-app:{{ tag_version }}"
      state: started
      ports:
        - "8181:8181"
      restart: yes
      networks:
        - name: phonebook_network
  tags:
    - preprod
    - prod

- name: Check that you can connect (GET) to a page and it returns a status 200
  uri:
    url: "http://{{ adresse_preprod }}:{{ port_preprod }}"
  tags:
    - test
